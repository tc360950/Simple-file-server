#include "server-consts.h"

namespace PARAMS {
  const uint64_t DEFAULT_MAX_SPACE =52428800 ;

   const int DEFAULT_TIMEOUT(5);

   const int MAX_TIMEOUT(300);

   uint64_t MAX_SPACE{52428800};

   int TIME_OUT{5};

   std::string SHRD_FLDR;

   int CMD_PORT;

   std::string MCAST_ADRR;

   const unsigned int MAX_UDP_PACKET_SIZE(65507);
}
